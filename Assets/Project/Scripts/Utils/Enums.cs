using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[System.Serializable]
public enum Face { Front, Right, Back, Left, Top, Bottom };

public enum Direction { Up, Down, Left, Right };
